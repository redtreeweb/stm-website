import React from 'react';

import Img from "gatsby-image"


export default (props) => {

    const {
        work_title,
        work_type,
        work_description,
        work_url,
        work_badges,
        work_thumbnail,
        action_button,
        index
    } = props;

   return <div></div>


}

